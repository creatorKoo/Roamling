# 요청 — 사용자가 원한다고 말한 것

**이 파일은 사용자 요청과 처리 상태의 정본이다.** 미완료 요청과 완료 근거를 함께 보존한다. 저장소의 다른 문서는 *지금 있는 것*
아니면 *지금 통과 중인 엔지니어링 게이트*를 적는다. 사용자가 말한 바람은 그 어디에도 들어갈
자리가 없어서, 그 판 계획서에 곁다리로 적혔다가 계획과 같이 묻혔다.

**계기 (2026-09-12).** 아내분이 2026-09-10에 요청한 색 팔레트 기능을 사용자가 다시 물었다.
저장소 전체에서 흔적은 한 줄이었다 — `output/plans/focus-activity.md`의 "§4. 변경하지 않을 것"
목록에 `색 팔레트(요청 B): 별도 플랜.` 그 파일은 git 미추적이고, "요청 B"가 무엇인지는 어디에도
적혀 있지 않았다. 같은 답변지에 있던 다른 요청 둘(숨기기, 끄기 발견성)은 흔적조차 없었다.

## 규칙

- 사용자가 원한다고 말하면 **코드보다 먼저 여기에 적는다.** 라벨(`요청 B`)로 참조하지 않고
  내용을 적는다.
- **원문을 남긴다.** 요약하면 그 사람이 실제로 원한 것이 사라진다.
- 상태는 셋뿐이다 — `대기` · `진행` · `완료`. 완료된 것은 한 줄 요약과 함께 아래로 내리고,
  자세한 것은 그 기능의 문서가 갖는다.
- **거절한 것도 남긴다.** 이유와 함께. 안 그러면 몇 달 뒤에 같은 논의를 다시 한다.
- **실사용에서 나온 결함도 여기 적는다.** 요청과 같은 이유다 — 고치기 전에 세션이 끝나면
  사라진다. 진단이 끝났으면 **어느 코드가 원인인지까지** 적는다. 고친 뒤에는 커밋과 그
  기능의 문서가 정본이 되므로, 여기서는 한 줄로 닫는다.

---

## 대기

없음.

---

## 진행

### R16. Windows에서 할 수 있는 리팩터를 전부 (2026-09-18)

- **누가·언제** 사용자, 2026-09-18, 0.6.7 발행 뒤.
- **원문** "그래 리팩터 할게 있으면 뭐가 있는지 보자 설명해줄래?", 이어서 "응 윈도우에서 할 수 있는거
  모두 하자. 내가 맥이랑 안드로이드에서 해줘야 하는게 있으면 알려줘 그거 하고 결과 여기에 알려줄게".
- **범위** `docs/maintenance-review.md`의 후보 2·3·4와 이번에 새로 본 큰 파일(`ffi.rs`,
  Windows `main.rs`). 전부 Rust라 Windows에서 끝내고 Swift는 push 뒤 CI로 확인한다. 리팩터 게이트라
  **동작·타이밍·기본값은 바꾸지 않는다** — differential fixture와 `RuntimeTrace`가 그대로 통과해야 한다.
- **순서** 3(배치 정책을 이름 붙은 타입으로) → 2(휘도 전달·`ClearanceMap` 재사용, **먼저 재고** 이득이
  없으면 하지 않는다) → 4(`pet_runtime.rs` 테스트·helper 분리) → `main.rs` 설정 저장부 분리.
  단계마다 흐름을 `docs/maintenance-review.md`에 먼저 적는다.
- **사용자가 해 줄 것** 맥·Android 확인 목록은 이 항목의 끝에 단계가 끝날 때 적는다.
- **상태** 진행.

### R15. 밀린 일 정리 — 리팩터 1과 B4, 맥에서 마무리 (2026-09-18)

- **누가·언제** 사용자, 2026-09-18, 0.6.6 발행 직후.
- **원문** "응 뭔가 밀린일 있으면 할까나", "1,3 으로 하고 2번은 드랍. 5번이랑 엮어서 아예 맥에서
  진행할까? 너의 의견은?", "ㄱㄱ".
- **결정** 밀린 일 다섯 중 B4(간헐 실패 테스트)와 리팩터 후보 1(접근 반응·잡기 이름 분리)을 한다.
  하늘 보리는 드랍(거절·보류 참조). 순서는 (1) 리팩터 1을 Windows에서 끝내고 push, (2) B4의 흐름을
  문서로 남기고, (3) 맥에서 B4 수정·하네스 반복 실행·서명 빌드, 이어서 macOS 실사용 확인(실제 클릭
  전달, 다중 모니터 경계 깜박임).
- **리팩터 1 (완료, Windows)** 흐름·이름 대응표·저장 키 결정은 `docs/maintenance-review.md`
  "리팩터 1 실행". Rust 내부 이름만 바꿨고 FFI·Swift·저장 키는 그대로다. 발견: Windows가 저장 키를
  enum 이름에서 유도하고 있어 이름을 바꾸면 사용자의 튠 값이 조용히 초기화될 뻔했다 —
  `RuntimeTuningKey::storage_name` 표와 코어·셸 테스트 둘로 고정했다. 패널 문구(en/ko)를 접근 반응
  설명으로 고쳤다. `scripts/test.ps1` 통과(코어 77, 셸 54, differential·릴리스 빌드), 펫 재실행.
  `2b7481b`로 push했고 [Check macOS](https://github.com/creatorKoo/Roamling/actions/runs/35312849337)·
  Windows·Android 세 검사가 통과했다(Swift 하네스 197개 포함).
- **B4 (맥에서, 수정됨)** 인수인계의 타이머 가설은 틀렸고 원인은 agent 이벤트의 미배달이었다.
  실측·수정·확인은 아래 B4 항목.
- **0.6.7 발행 (2026-09-18, 사용자 "잘 된다 커밋하고 버전 발행해줘")** B4 `fc8f573`, B5 `8e1f8ad`,
  버전 `055ddb6`. 조작·기능 변화가 없어 사용 안내 revision은 유지했다. push 뒤 Check macOS·Windows·
  Android 셋이 통과한 다음 태그를 밀었고, 릴리스 워크플로의 macos·windows·publish 잡이 **다시 돌리지
  않고 한 번에** 통과했다 — 0.6.6에서 B4로 떨어졌던 macOS Test 잡 포함(하네스 198개).
  [v0.6.7](https://github.com/creatorKoo/Roamling/releases/tag/v0.6.7)에 공개 파일 6개가 올라갔고
  `latest` 피드가 0.6.7 두 플랫폼 URL을 가리킨다. **남은 것은 R15의 macOS 실사용 확인 둘**(실제 클릭
  전달, 다중 모니터 경계 깜박임)과, 설치판(`/Applications/Roamling.app`)이 업데이트로 0.6.7을 받는지다.

### R6. Android에서 돌아다니는 Roamling

- **누가·언제** 사용자, 2026-09-12. 첫 사용 대상은 아내분의 Android 휴대전화.
- **원문** "지금 이 프로젝트를 안드로이드로 개발할 수 있니? 완전히 똑같은 기능은 아니더라도
  돌아다니게 해서..?!" 이어서 "아이폰도 노치에 라이브 액티비티랑 잠금화면 위젯으로 해서,
  잠겨있을 때와 해제시에는 노치에 가있고 이렇게 하면 얼추 되긴하겠다"와 "안드로이드도
  잠금화면에서 뭔가가 되니?"라고 물었다. 마지막으로 "일단 아내 안드로이드폰이라 이거를
  기반으로 초기 프로젝트 시작할 수 있게" 방향을 문서로 남겨 달라고 했다.
- **판단** Android의 첫 목표는 데스크톱판과 완전히 같은 기능이 아니라, 잠금 해제 중 다른 앱 위를
  모치가 자연스럽게 돌아다니는 경험이다. 잠금 중에는 오버레이를 억지로 유지하지 않고 위젯이나
  선택형 라이브 배경화면으로 이어 보인다. 기존 Rust 코어와 승인된 Mochi 그림을 재사용한다.
- **방향** `docs/android.md`.
- **개발 환경 요청 (2026-09-16)** "아내폰은 갤럭시 s25?26? 플러스 최신 오에스 일듯",
  "내 테스트폰은 있긴한데 시뮬레이터로 했음 좋겠어 항시 연결 불가해서".
  정확한 모델과 Android·One UI 버전은 미확인이다. 일상 개발·반복 검증은 Android 에뮬레이터를
  기본으로 하고, 삼성 기기의 잠금·복귀와 배터리 제한은 실기기를 연결할 수 있을 때 확인한다.
- **환경 준비 승인 (2026-09-16)** "나 2시간동안 원격으로 작업해야하는데 진행 가능한 범위까지만
  진행해줄래?". 도구 설치·파일 준비·CLI 검증을 진행하되 원격 작업 중 재부팅, Windows 기능 변경,
  BIOS 설정과 화면을 차지하는 IDE·에뮬레이터 실행은 미룬다. 앱 구현 착수와는 별도다.
- **환경 준비 결과 (2026-09-16)** Windows 도구 설치·무창 에뮬레이터 부팅·adb 연결·임시 Rust
  ARM64/x86_64 빌드 확인 후 테스트 기기를 종료했다. 재부팅·Windows 기능 변경 없음.
  설치 경로와 확인 범위는 `docs/android.md`의 "Windows 환경 준비 결과". 이 시점에는 환경만 검증했다.
- **착수 승인** "응 한번 커밋하고 다음 진행하자". 환경 준비 기록을 커밋한 뒤 A0 최소 APK와
  기존 Rust 코어 호출을 구현·검증한다. A1의 모치 표시와 상시 서비스는 다음 단계다.
- **설계** `docs/android.md`의 A0 범위로 진행. macOS와 사용자 확인을 자동 검증으로 대신하지 않는다.
- **검증 결과 (2026-09-16)** Windows에서 A0 APK 빌드, Android 17 x86_64 에뮬레이터의
  기존 Rust `PetLoop` 한 틱 실행과 좌표 로그 확인. 공통 코어·Windows 회귀 검사 통과.
  에뮬레이터를 종료하고 원래 데스크톱 펫을 복원했다. 상세는 `docs/android.md`의 A0 결과.
- **A1 착수 승인** A0 결과 보고 뒤 "커밋하고 다음으로 가자". A0를 `e3a4d45`로 커밋하고
  Windows 에뮬레이터에서 A1 표시를 진행한다. A0의 macOS 미검증을 완료로 바꾸지는 않는다.
- **A1 결과 (2026-09-16)** 권한 화면·오버레이·기존 Mochi idle 미리보기 구현. Windows에서 APK·브리지·
  Android 계측 검사와 데스크톱 회귀 검사 통과. 권한 거절/승인 복귀, 숨김·회전·홈 이동과 실제 캡처 확인.
  상세 결과·재현 명령·화면은 `docs/android.md`의 A1 결과. 검사 후 에뮬레이터 종료·설치본 펫 복원.
- **A2 착수 승인 (2026-09-17)** 보리 이름 변경·0.6.4 발행 뒤 Android 다음 단계 안내에
  "응 다음 진행 고고". Activity 안에서 걷기·쉬기·직접 터치 드래그를 구현하고 에뮬레이터로 검증한다.
  홈 유지·서비스·잠금 복귀는 A3에 남긴다. 구현 흐름은 `docs/android.md`의 A2 절.
- **A2 결과 (2026-09-17)** 공유 코어 걷기·휴식·직접 터치 드래그와 회전 위치 복원을 연결했다.
  Android 17 에뮬레이터 계측 3개, A0 smoke, APK·lint·16 KB 정렬과 Windows 전체 게이트 통과.
  잠들기·깨우기는 기본 튜닝을 유지한 테스트 시계로 검증했다. 상세는 `docs/android.md` A2 결과.
- **A3 착수 승인 (2026-09-17)** A2 결과 뒤 "응 커밋하고 다음 진행". A2를 `66775b2`로
  커밋하고 A3 홈/다른 앱 유지·알림 제어·잠금 중단/해제 복귀를 진행한다. 자동 검증을
  삼성 실사용 확인으로 바꾸지 않는다. A3 구체 흐름은 `docs/android.md`.
- **A3 결과 (2026-09-17)** 서비스가 창을 소유하도록 옮기고 상시 알림·숨김/종료·잠금 복귀와
  위치 저장을 구현했다. Android 17 에뮬레이터 계측 5개, APK·lint·16 KB 정렬 통과.
  실제 알림 권한 거절/허용과 홈 화면·알림 버튼도 확인했다. 상세는 `docs/android.md` A3 결과.
- **상태** 진행 — A3 사용자 체감 확인 대기. Samsung 실기기·배터리와 A0 macOS 검증은 남아 있다.
- **A3 커밋 승인 (2026-09-17)** "밖이라 있다가 저녁에 가능. 지금 커밋하고 더 진행가능하니?
  아님 테스트할때까지 대기?". 검증된 A3를 커밋하고 저녁 테스트 순서를 정리한다.
  다음 기능 게이트는 사용자 체감 확인 뒤 진행한다.

---

## 완료

### R14. 색 프리셋 이름에 보리를 붙이기 (2026-09-18)

- **누가·언제** 사용자, 2026-09-18.
- **원문** "이거 이름을 바꾸고 싶은데 설계해줄래? 기본 : 보리 보리 / 검정: 검은 보리 / 하양: 하얀 보리",
  "대안안이 훨씬 귀엽고 웃기다 다만 보라 보리 핑크는 핫핑크 보리로 해줘 영어도 색을 대안 색으로 해줘",
  "파란 보리로 하자 그럼 하늘 보리는 나중에 추가하고 그리고 한번에 해서 발행까지 해줘".
- **결정** 아홉 프리셋의 표시 이름만 바꾼다. 키·프리셋 값·저장 형식은 그대로다.

  | 키 | ko | en |
  |---|---|---|
  | `palette.default` | 보리 보리 | Bori Bori |
  | `palette.black` | 흑보리 | Black Bori |
  | `palette.white` | 흰 보리 | White Bori |
  | `palette.red` | 붉은 보리 | Crimson Bori |
  | `palette.hotpink` | 핫핑크 보리 | Hot Pink Bori |
  | `palette.yellow` | 황금 보리 | Golden Bori |
  | `palette.blue` | 파란 보리 | Blue Bori |
  | `palette.purple` | 보라 보리 | Purple Bori |
  | `palette.rainbow` | 알록달록 보리 | Motley Bori |

  R8의 "새 색상별 이름은 만들지 않는다"는 이 결정으로 뒤집혔다. 파랑은 실제 프리셋 명도가
  22–78로 하늘색보다 진해서 "파란 보리"로 두고, **하늘 보리는 밝은 파랑 프리셋을 새로 만들 때
  붙일 이름으로 남겨 둔다.**
- **범위** ko/en `Localizable.strings`의 `palette.*` 아홉 키와 팔레트 창 footer의 "펫 › 보리 › 기본"
  문구. Windows 트레이는 같은 키를 읽으므로 따로 고칠 것이 없다. 사용 안내 revision은 조작·기능
  변화가 아니라 유지한다(`docs/usage-guide.md`).
- **상태** 완료. `scripts/test.ps1` 통과, Windows 라이브 트레이 메뉴(HMENU)에서 아홉 이름과
  저장돼 있던 흑보리 체크가 그대로인 것을 읽어 확인했다. `9b442f8`로 커밋, 세 검사 워크플로
  통과 뒤 [v0.6.6](https://github.com/creatorKoo/Roamling/releases/tag/v0.6.6)을 발행했다.
  공개 파일 6개가 올라갔고 `latest` 피드가 0.6.6 두 플랫폼 URL을 가리킨다. 릴리스의 macOS
  Test 잡이 한 번 B4로 떨어져 실패 잡만 다시 돌렸다. 사용자의 실제 UI 확인은 별개다.

---

### R13. 첫 사용·주요 기능 변경 시 짧은 사용 안내 (2026-09-18)

- **원문** "이걸 한번 설명하는 창을 넣을까나? 뭔가 버전 업데이트 되거나 처음 인스톨 한
  사용자에게 대략적으로 이런기능이 있고 이렇게 사용할 수 있어요 이런 팝업창 한번이면
  될 것 같은데. 이것도 컨트롤로 해결하는게 맞을 것 같고.. 너는 어떻게 생각하니?"
- **맥락** 직접 클릭은 가능해도 회피 때문에 잡기 어려웠다. 왼쪽 Ctrl(Windows) / 왼쪽
  Command(macOS)의 애정 키를 알게 된 뒤, 회피 유예를 새로 넣기보다 사용법을 안내하는 방향을 제안했다.
- **승인된 방향** 최초 실행에는 조작·설정 진입점을 한 화면으로 안내한다. 매 패치가 아니라
  사용자에게 알릴 조작·기능이 바뀐 업데이트에서만 한 번 다시 표시하고, 트레이의 사용법 메뉴로
  언제든 다시 연다. 작업을 막지 않는 짧은 modeless 창을 사용한다.
- **현재 코드와 연결점** `RoamlingAppDelegate.offerLaunchAtLoginOnce`는 로그인 등록용으로
  이미 본 안내를 기록하는 기능이 아니다. 첫 안내·기능 안내 상태는 별도로 관리한다.
  `ShellMenu.MenuAction` / Windows `tray`에 다시 보기 진입점을 연결했고 플랫폼별 키를 표시한다.
- **사용자 승인** 안내 버전은 앱 버전과 별도로 올리고 릴리스 빌드에 문구와 함께 포함한다.
  처음에는 기본 안내, 기존 사용자에게는 아직 보지 않은 변경만 한 창에 모은다. 기본 네이티브
  UI를 사용하며 구현 전후 실행 파일 크기를 비교한다. 0.3초 회피 유예는 추가하지 않는다.
- **상태** 완료. Windows 사용자 확인과 양 플랫폼 테스트·빌드·서명된 Mac 앱 실행 검증을 거쳐
  [v0.6.5](https://github.com/creatorKoo/Roamling/releases/tag/v0.6.5)를 발행했다.
  공개 파일 6개의 SHA-256과 업데이트 피드·양 플랫폼 업데이트 파일의 Ed25519 서명도 검증했다.
  실행 파일 증가는 로컬 비교에서 20.5 KiB(0.65%). 구현·릴리스 절차는 `docs/usage-guide.md`.
- **발행 승인 (2026-09-18)** "굿 좋아 버전발행 승인". 사용자가 안내 창을 확인했다.
  잡기·꼬리 반응·경계 이동·내용 회피 개선과 안내를 0.6.5에 포함하고, 양 플랫폼 검증 후 발행한다.
- **녹화 기준 갱신 승인 (2026-09-18)** "변경 차이 검토 후 기준 갱신 승인". 기존 녹화를 보존하고
  전체 차이가 승인된 동작 변경인지 검토한 뒤 0.6.5 녹화 기준을 갱신한다. 수동 CI는 검토용
  녹화만 산출하며 fixture를 자동 교체하거나 릴리스를 발행하지 않는다.

---

### R8. 내장 마스코트 표시 이름을 보리로 변경

- **누가·언제** 사용자, 2026-09-17.
- **원문** "좋다 보리", "일단 모찌를 보리로 고치는 걸루 가자".
- **발행 승인 (2026-09-17)** "응 커밋하고 github에서 mac 검증해보고 잘 되면 버전 발행하자".
  GitHub macOS 검증 통과 뒤 다음 패치 버전을 발행한다. Android는 A1 개발 미리보기이며 정식 배포물은 아직 아니다.
- **범위** macOS·Windows·Android의 기존 마스코트 표시 이름과 안내를 한국어 `보리`, 영어 `Bori`로
  맞춘다. 기존 파생 이름의 `Mochi` 표기도 `Bori`로 바꾸되 새 색상별 이름은 만들지 않는다.
  저장된 선택과 자산 경로를 보존하기 위해 `mochi` 식별자·파일명·바인딩 API는 유지한다.
- **흐름** `MascotPetFactory`·`PlaceholderPetFactory`·`roamling-pet`의 표시 메타데이터 →
  `ShellMenu`·Windows `tray`의 현지화된 내장 이름 → 팔레트 안내와 Android 문자열.
  설치된 외부 펫의 이름은 해당 패키지 값을 그대로 표시한다. README와 현재 메뉴 안내도 맞춘다.
- **상태** 완료. Windows 공통·메뉴 회귀 검사와 release 빌드, Android 두 ABI 빌드·APK·lint 통과.
  `1349475`로 커밋했고 [GitHub macOS 검증](https://github.com/creatorKoo/Roamling/actions/runs/35114221148)도
  테스트·서명·패키징·실행까지 통과했다. 승인에 따라 `0.6.4` 릴리즈를 준비한다. 실제 사용자 UI 확인은 별개다.

### R7. 선택한 색 기억과 Mochi 하위 색 선택

- **누가·언제** 사용자, 2026-09-15.
- **원문** "색 선택하고 저장이 안되는데 저장 기능 추가해줄래?"
- **추가 확인** 재실행 후 검정색 초기화는 구버전 사용 때문이었으며, 사용자 본인이 업그레이드 후 정상이라고 확인했다.
- **정정 원문** "아니 이렇게 색 저장이 나오지 말고 내가 선택한것이 껐다가 켜도 선택되었으면 좋겠고 색 밑으로 들어가지 말고 모찌 하위로 색 선택이 들어갔으면 좋겠어"
- **범위** 별도 저장 버튼·내 색 목록을 제거한다. 선택한 색은 자동으로 기억하고, Windows의 색 프리셋과 색 직접 고르기를 `펫 › Mochi` 바로 아래에 둔다. 색을 고르면 Mochi가 선택된다.
- **상태** 완료. 사용자 정정에 맞춰 구현하고 `scripts/test.ps1` 통과(색 자동 저장·재시작 후 복원, Mochi 하위 메뉴·체크 표시 포함). 2026-09-16 사용자 발행 승인. 화면 자동화 도구 실행 실패로 에이전트의 육안 확인은 하지 못했다.

### R4. 고양이 한 마리 더 (검은 고양이) — 완료 (2026-09-14)

- **누가·언제** 아내분, 2026-09-10 (설문 10번)
- **원문** "안지움. **검은색 고양이 한마리 더 있었으면 좋겠다고 생각함.**"
- **결과** R1이 흡수했다. 검은 고양이는 리컬러 **프리셋 하나**이고 양 플랫폼에 실려 있다.
  새 시트를 그리지 않았다 — 승인된 Mochi 프레임 그대로에 색 가족만 옮긴다.
  **"검은 고양이 시트를 새로 그려야 하나"로 다시 논의하지 않는다.**
- **본인이 R1을 먼저 하자고 했고 그 판단이 맞았다.** 한 벌의 리컬러가 아홉 마리를 냈다.
- **"한 마리 더"는 두 마리를 동시에 띄우자는 뜻이 아니었다** (사용자 확인 2026-09-14).
  검은 고양이가 있었으면 해서 나온 말이고, 검정 프리셋으로 충분하다. 다중 펫은 요청된 적이 없다 —
  나중에 누가 꺼내면 그때가 첫 요청이다.


### R1. 색 팔레트로 내 고양이 만들기 — 양 플랫폼 완료 (2026-09-14)

- **누가·언제** 아내분(초등교사, Windows), 2026-09-10
- **원문** "새로운 고양이도 좋은데 일단 **색을 내 마음대로 바꾸는 기능**이 있었으면 좋겠데.
  뭔가 **색상 팔레트를 내가 맘대로 조정해서 원하는 고양이를 만드는 기능**이 있었으면 좋겠다고
  했어. 막 무지개색 고양이? 이러면서 장난치기도 했어."
- **결과** 프리셋 아홉(무지개 포함), OS 색 피커, 축마다 슬라이더가 있는 창. **양쪽 다 있고**
  리컬러·프리셋 값·설정 형식은 공유 Rust 한 벌이다. 갈린 자리는 `docs/palette.md`.
- **확인** Windows 2026-09-13, macOS 2026-09-14. 둘 다 사용자가 직접 써 보고 확인.
- **범위를 두 번 좁게 잡았다** — 처음엔 "macOS는 프리셋만", 다음엔 "색 고르기까지". 이유는
  같다: **Windows의 색 고르기는 우리 창이 아니라 OS 대화상자**였고, 창을 짓는 값도 macOS에서는
  기존 SwiftUI 관용구 덕에 Win32의 그것과 전혀 달랐다(약 200줄).
  **다른 플랫폼의 작업량을 그 플랫폼의 코드 줄 수로 짐작하지 않는다.**
- **곁가지로 기본 내장 펫이 Mochi가 됐다** (사용자 결정 2026-09-14). FatMochi는 손으로 그린
  7행 시트라 색이 안 닿는데, 기본값이 그것이면 이 기능이 처음부터 안 보인다.
  FatMochi는 나중에 교체할 것이라 "지금은 없는 셈"으로 두었다.


### R5. 일하는 앱 반응 — macOS 확인됨 (2026-09-12)

- **누가·언제** 아내분, 2026-09-10
- **원문** "내가 클로드 일하는 모습을 보면서 **왜 나는 이런 행동안해?** 이렇게 물어봐서 뭔가
  일상 생활에서도 있었으면 좋겠어. 한글과컴퓨터의 한글이 맨위에 올라가 있으면 주로 일하는거래.
  그리고 한쇼, 한셀, 한글 이렇게 3개 사용한데"
- **결과** `CLAUDE.md`의 G 항목. 2026-09-11 사건형으로 구현 → 2026-09-12 상태형으로 재구성.
  흐름은 `docs/behavior-flow.md` §5b.
- **확인** 2026-09-12, 사용자가 서명 빌드로 직접 써 보고 확인. **exit rule 통과** — 이제
  다음 게이트로 갈 수 있다.
- **Windows 배선도 닫혔다 (2026-09-12).** `docs/windows.md` W8 ✅. 요청한 본인이 Windows
  사용자였으므로 이걸 기다리느라 v0.4.1 뒤가 발행되지 않고 쌓여 있었다. **v0.5.0에 양 플랫폼이
  같은 판으로 실린다** — 일하는 앱, 상태형 재구성, 제자리 뛰기 수정(B1), 그리고 아래 R2·R3.

### R2. 잠깐 숨기기 — 완료 (2026-09-12)

- **누가·언제** 아내분, 2026-09-10 (설문 5번)
- **원문** "있음. **애들 잡도리할때 화면만 쳐다봄. on.off 기능이 있었으면..?**"
- **결과** 메뉴 맨 위의 `잠깐 숨기기`. **창만 숨긴다** — 펫은 살아 있고 agent 이벤트도 계속
  받으므로 가려 둔 30초 사이의 일을 잃지 않는다. 설계와 근거는 `docs/hiding.md`.
- **확인** 2026-09-12, 사용자가 실물로 확인.

### R3. 끄는 법을 못 찾는다 — 완료 (2026-09-12)

- **누가·언제** 아내분, 2026-09-10 (설문 8번)
- **원문** "있음. **다른건 안 눌러보고 끄기를 못 찾았다고 함**;"
- **결과** 기능 추가가 아니라 **메뉴를 줄였다.** 나머지를 `움직임 ▸` · `인식 ▸` · `고급 ▸`
  셋으로 접어 고를 수 있는 줄이 **19개 → 8개**가 됐고, 종료는 관례대로 맨 끝에 있으면서 8번째가
  됐다. agent 수와 무관해진 것도 이때다.
- **확인** 2026-09-12. 돌고 있는 앱의 살아 있는 메뉴를 읽어 8개를 확인했다(`docs/hiding.md`).

---

## 결함 — 실사용에서 나온 것

### B1. 제자리 뛰기 — 서 있는 자리로 산책을 나간다 (고침 2026-09-12)

- **누가·언제** 사용자, 2026-09-12. macOS 서명 빌드로 쓰던 중 목격.
- **증상** 펫이 걷는 그림을 입은 채 움직이지 않는다. 진단 로그에서 약 6초.

  ```
  8739.9  place stroll to 170,898   → 걸어가서 도착
  8742.3  pet idle
  8792.6  place stroll to 170,898   ← 이미 거기 → 정지
  8794.6  place stroll to 170,898   ← 또
  8796.6  place stroll to 170,898   ← 또
  8798.6  place stroll to 183,839   ← 다른 점이라 실제로 걸음
  8800.0  pet idle
  ```

  `pet wander`가 8792.6에 한 번만 찍혔다 — 진단은 바뀔 때만 적으므로 그 뒤로 계속 걷기
  상태였다는 뜻이다.

- **원인 (코드로 확정)** `pet_runtime.rs`의 `begin_stroll`이 **걷기 상태를 먼저 잡고 그다음에
  경로가 없다는 것을 안다.** 경로가 비면 `next_wander_at = now + 2.0`만 두고 **상태를 되돌리지
  않는다.** 경로가 비는 이유는 사슬이다:
  1. 같은 디스플레이 안이면 `route()`가 목적지 한 점만 돌려준다 (`topology.rs`)
  2. `set_route`가 곧바로 `skip_reached_waypoints()`를 부르고, 그 점이 `arrival_radius` 안이면
     소비한다 (`movement.rs`)
  3. `has_route()` 거짓 — 서 있는 자리로 걸으라고 한 것이다

  그 함수의 주석은 반대 이유를 적고 있다("경로를 먼저 놓았더니 상태 전이가 거부됐을 때 idle
  그림으로 걸었다"). **거울상 버그를 만든 것이다.**

- **왜 같은 좌표인가 (추정, 미확정)** `170,898`이 한 세션에 8번 나온다.
  `random_wander_point`의 마지막이 `safe.closest_point(...)` 클램프라, 안전 영역 밖 목표가 전부
  같은 모서리로 눌린다. y 샘플링이 72% 확률로 아래쪽 띠를 고르므로 왼쪽 아래가 자주 걸린다.
  로그와 일관되지만 런타임 값 없이 확정하지 못했다.

- **고침** 2026-09-12, 둘 다.
  - **A.** 경로를 먼저 만들고, 걸을 것이 있을 때만 걷기 상태를 잡는다. 상태 전이가 거부되면
    경로를 취소한다 — 그래야 옛 결함(idle 그림으로 걷기)도 같이 막힌다.
  - **B.** `arrival_radius` 안의 후보는 산책 목적지가 아니므로 후보 단계에서 버린다. 추첨
    자체는 그대로 여섯 번 돌리고 **뒤에서 거른다** — 난수 순서가 바뀌면 differential 픽스처가
    흔들린다.
- **회귀 테스트** `a stroll to where the pet already stands never wears the walk`
  (`RuntimeLogicTests.swift`). 펫(96×104)보다 작은 120×110 화면을 주면 `safe`가 비어
  `random_wander_point`가 **화면 중앙만** 돌려주므로 결함이 결정적으로 재현된다. 커서는 화면
  **밖 멀리** 둬야 한다 — 그렇게 작은 화면에선 어느 점이든 인식 거리 안이라 펫이 회피만 하고
  결함에 닿지 않는다(처음 두 번 이걸로 헛통과했다).
  **고치기 전 코드에서 실패하는 것을 확인했다** — `117.8 s` 동안 제자리 걷기.
- **게이트 결과** `RuntimeTrace.txt` 바이트 불변, differential 픽스처 무변경, 185개 통과.

### B2. 글자 위에 계속 앉는다 — 캡처가 한 번 끊기면 안 돌아온다 (고침 2026-09-15)

- **누가·언제** 사용자, 2026-09-15. 화면 셋을 붙인 macOS 서명 빌드. "화면인식이 잘 안되는거
  같은데 … 글자위에 계속 앉아있어." 화면 기록 권한은 켜져 있었다.
- **증상** 캐럿은 피하는데 글자는 못 피한다. 진단 로그에서 한 프로세스가 `294.7 capture
  authorised, none yet` 뒤로 **2시간 21분** 동안 필드를 못 받았고, 0.6.0 자동 업데이트가 새
  프로세스를 켜자 0.2초 만에 됐다. 옛 프로세스는 스스로 회복한 적이 없다.
- **원인** 진단과 확정·미확정의 경계는 `docs/capture.md` §2. 요약하면 셋이 겹쳤다 —
  실패 네 가지가 전부 `nil` 하나로 합쳐져 **무엇이 실패했는지 로그가 말할 수 없었고**, 매달린
  요청에 **시한이 없어** 한 번 안 돌아오면 이후 요청이 전부 막혔으며, 실패 한 번이 **직전의
  멀쩡한 필드를 지웠다.**
- **고침** 2026-09-15, `docs/capture.md` §3의 셋. 실패에 이름을 붙이고 `capture-request` 범주에
  적는다 · 틱에서 5초 시한을 재고 세대 번호로 늦은 답을 버리며 연속 만료에는 backoff ·
  실패가 캐시를 지우지 않되 마지막 성공이 12초를 넘으면 코어에 `None`을 준다. 시작마다
  `launch pid=… version=…` 한 줄을 파일에 적어 실행 경계가 보이게 했다.
- **회귀 테스트** `RuntimeLogicTests.swift`의 넷 — 안 돌아오는 캡처가 다음 요청을 막지
  않는다 · 버린 요청의 늦은 답은 아무것도 바꾸지 않는다 · 실패는 직전 필드를 지키되 오래
  가지 않는다 · 같은 실패는 한 번만 적힌다.
- **게이트 결과** `RuntimeTrace.txt` 바이트 불변, differential 픽스처 무변경, 195개 통과,
  Windows 셸 `cargo check` 통과.
- **아직 모르는 것** 그날 실제로 어느 단계가 왜 실패했는지. 새 로그가 처음으로 그것을 말해
  줄 것이다 — 다시 보이면 `/tmp/roamling-rest.log`의 `capture-request` 줄을 본다.

---

### B3. Windows Claude Code 훅이 프로젝트에 NUL 파일을 만든다 (2026-09-16)

- **요청** Git Bash에서 매 훅 호출마다 생기는 0바이트 `NUL` 파일을 막고, 훅 재설치 뒤에도
  수정이 유지되어야 한다. 사용자는 `>/dev/null 2>&1`로 바꾼 뒤 재생성이 멈췄음을 확인했다.
- **원인** `rust/roamling-agent/src/installer.rs::command`와 Swift `HookCommand.silencer`가
  Windows라는 이유로 `>NUL 2>&1`을 썼다. 파일을 여는 주체는 이 경우 Git Bash다.
- **고침** 셸 리다이렉션을 없애고 Windows 네이티브 `curl.exe --silent --output NUL`이
  출력을 버리게 한다. PowerShell 대체 실행도 같은 명령을 쓴다. 구형 훅은 기존 `is_current`와
  `strip` 경로로 식별·교체하고, 사용자 설정과 다른 도구의 훅은 보존한다.
- **회귀 테스트** `rust/roamling-agent/tests/windows_hook_shells.rs`에서 실제 Git Bash와
  PowerShell로 JSON 전달·응답 폐기·수신기 종료 시 무출력·cwd 무변경을 확인한다.
  `installer.rs::tests::legacy_nul_redirections_are_repairable_for_every_event`는 구형 명령의
  식별·복구 필요 판정과 다른 설정·훅의 보존을 확인한다.
- **검증** Windows `scripts/test.ps1` 통과(셸 51개 통과, 네트워크 테스트 1개 제외),
  릴리스 빌드와 앱 재시작 완료. Swift Windows 분기는 소스만 수정했고 컴파일하지 않았다.

---

### R9. 앉아 있을 때만 응시하고 모니터 경계는 이어서 통과 (2026-09-17)

- 요청: Codex의 `running` 작업 중 가까운 마우스 때문에 꼬리를 흔들지 않는다.
  이동 애니메이션의 달리기를 뜻하는 것이 아니다. 모니터 경계는 정착·휴식 장소로
  삼지 않고, 건너기 시작하면 안쪽까지 이어서 이동한다. macOS 경계 왕복 시 깜박임 체감 보고.
- 추가 요청: 일반 커서에 대한 꼬리 흔들기는 가만히 앉아 있을 때만 한다. 배회·목적지 이동
  도중 커서 근처를 지난다는 이유로 갑자기 멈춰 꼬리를 흔들지 않는다.
- 흐름: `ActivityDirector::sustained_reaction`의 `Work` →
  `PetRuntime::finish_tick`의 근접 응시 → `make_situation`의 응시 소유권 →
  `hold_seat`의 작업 반응 유지. 일반 응시는 `Idle`·`Sit`·`LookAtPointer`이며 경로가 없고
  속도가 1pt/s 이하인 경우만 허용한다. 회피·직접 잡기·명시적 애정 키는 유지한다.
- 경계: `make_situation`·`begin_rest_travel`·`random_wander_point`에서 사용하는
  배치 영역에 연결된 가장자리 여유를 주며 실제 화면 좌표와 통행 경로는 유지한다.
  `DisplayTopology::route`의 경계 중간 지점은 `MovementController`에서 정차 지점으로
  취급하지 않고, `finish_tick`은 경계 진입부터 이웃 화면 안쪽까지 이동을 유지한다.
- 검증 범위: 공유 Rust 코어와 Windows 게이트. macOS 실제 다중 모니터 시각 검증은 별도 필요.
- 결과: 코어 단위 테스트 64개와 기존 differential 검증 통과, Windows 셸 51개 통과
  (기존 네트워크 테스트 1개 제외), `scripts/test.ps1` 및 릴리스 빌드 성공.
  fixture·`RuntimeTrace.txt`는 변경하지 않았다.

### R10. 글씨에서 가능한 한 떨어진 자리인지 재검증 (2026-09-17)

- 요청: 글씨에서 최대한 떨어진 곳에 위치하기를 원하며, 현재 구현이 그 목표에 맞는지 검증한다.
- 점검 경로: `VisualEmptiness::most_comfortable`의 배회 여유 등급,
  `BasicInterestPositionPlanner::evaluate`의 작업 좌석 점수,
  `BasicSafeZonePlanner::destination`의 휴식 후보와 `PlacementDirector::departure_reason`의
  자리 재평가. 최초 점검에서는 동작을 변경하지 않고 기존 선택을 재현했다.
- 수정 전 결과: 배회는 여유 등급이 같은 후보 사이의 거리를 구분하지 않고, 작업 좌석에는 여유 등급이
  적용되지 않는다. 휴식에서는 빈 모서리가 있어도 busy 모서리를 선택해 `Sleep`에 들어가는
  런타임 재현에 성공했다. `docs/placement.md` 3.2.3에 입력·수치·개선 방향을 기록했다.
- 검증: 합성 격자 재현 assertion 모두 통과, 기존 emptiness·interest·placement·world
  differential 테스트 네 묶음 통과. 실제 macOS 화면 검증은 수행하지 않았다.
- 사용자 승인: 같은 날 권고안대로 수정하기로 확정. 공유 코어의 런타임 경로에서 배회·작업·
  휴식 후보를 같은 내용/여유 기준으로 고르고, 휴식 도착 직전에도 내용 덮임을 확인한다.
  체류 시간·이동 거리·재평가 주기는 유지하며, 화면을 판단할 수 없을 때는 기존 무권한
  배치를 유지한다. 공개 FFI 계약과 기존 포팅 fixture는 변경하지 않는다.
- 구현: `ClearanceMap`으로 내용 겹침을 제외하고 몸체와 내용 사이의 거리를 24pt 구간으로
  비교한다. 하단·캐럿 근접 가점은 같은 거리 구간에서만 적용한다. 휴식은 모서리와 7×5 격자를
  비교하고, 도착 후와 수면 중 새 캡처에서도 다시 검사한다. 빈 후보가 없으면 깨어 있고
  30초 뒤 휴식을 재시도한다. 기존 체류·재평가 조건과 캡처 주기는 유지한다.
- 수정 후 검증: `scripts/test.ps1` 통과. 새 회귀 8개를 포함한 코어 단위 테스트 72개,
  기존 differential 검증, Windows 셸 51개 통과(기존 네트워크 테스트 1개 제외).
  릴리스 빌드 후 펫 재실행과 실행 경로를 확인했다. fixture·`RuntimeTrace.txt`는 변경하지
  않았다. macOS 컴파일과 실제 화면에서의 시각 검증은 이 Windows 환경에서 수행하지 않았다.

### R11. 접근 속도와 무관하게 몸체 클릭으로 잡기 (2026-09-18)

- 요청과 승인: 달리는 펫이 잘 잡히지 않는다는 실사용 피드백. 몸체를 클릭하면 이동·작업 상태나
  커서 접근 속도와 관계없이 잡도록 변경하기로 승인했다. 일반 꼬리 흔들기의 앉은 상태 제한은 유지한다.
- 원인: `PointerInteractionModel::evaluate`가 빠른 접근에만 잡기 창을 열고,
  `PetRuntime::finish_tick`의 `interaction_enabled`와 `pointer_down`이 모두 이 창을 요구한다.
  이동 중 응시로 멈추지 않게 되면서 짧은 창 안에 움직이는 몸체를 클릭하기가 더 어려워졌다.
- 변경 흐름: 셸의 몸체 판정 → `finish_tick`이 접근 속도 없이 클릭 전달 허용 →
  `pointer_down`이 `touch_down`과 같은 직접 접촉 검증 → `begin_catch` → 기존 끌기·놓기.
  숨김·상호작용 끔·몸체 밖·이미 잡힌 상태는 거절하고, 클릭 전에는 이동과 작업을 유지한다.
  Windows `set_click_through`와 macOS `setInteractionEnabled`가 같은 코어 출력을 사용한다.
- 검증: 실제 접근 입력부터 클릭·끌기·놓기까지 연결한 회귀 테스트와 Windows 표준 게이트를 실행한다.
  기존 fixture와 트레이스는 재생성하지 않는다. macOS의 실제 클릭 전달은 별도 실기기 검증 대상이다.
- 결과: 코어 75개(직접 클릭 회귀 3개 추가), 기존 differential 검증, Windows 셸 51개 통과
  (기존 네트워크 테스트 1개 제외). 최종 `scripts/test.ps1`과 릴리스 빌드 통과 후 앱을 재실행했다.

### R12. 문서 전체 최신화와 리팩터링 후보 검토 (2026-09-18)

- 요청: 직접 클릭 수정을 마친 뒤 전체 문서를 최신화하고 필요한 리팩터링이 있으면 알린다.
- 범위: 추적 중인 Markdown 전체의 로컬 링크와 오래된 동작·구현 상태 표현을 검색하고,
  현재 README·설계·동작·캡처·플랫폼 안내를 코드와 대조한다. 과거 게이트의 실측·검증 기록은
  현재 동작으로 덮어쓰지 않는다. 리팩터링은 근거와 우선순위를 제안하며 큰 구조 변경은 하지 않는다.
- 결과와 후속 후보: `docs/maintenance-review.md`. 실제 macOS·Android 실기기 검증이나
  원격 CI를 새로 실행한 것으로 기록하지 않는다.
- 검증 중 보완: 기존 훅 셸 테스트의 `WouldBlock` 실패가 Git Bash와 PowerShell에서 각각
  재현됐다. `windows_hook_shells.rs::check_shell`의 수신 stream을 명시적으로 blocking으로
  설정해 기존 3초 읽기 제한으로 데이터를 기다리게 했다. 제품 훅 명령은 변경하지 않았다.
- 최종 검증: 훅 셸 테스트 10회 연속 통과 뒤 전체 게이트 통과. Markdown 51개·로컬 링크 49개와
  코드·문서의 문서 경로 참조 27개를 검사하여 누락 0개. `git diff --check` 통과.

### B4. CI에서 한 번씩 떨어지는 Swift 하네스 테스트 (2026-09-18)

- **어디서** v0.6.6 릴리스의 macOS Test 잡. 같은 커밋 `9b442f8`이 몇 분 전 Check macOS에서는
  197개 전부 통과했고, 실패한 잡만 다시 돌리자 통과했다.
- **무엇이** `FocusActivityLogicTests.swift`의 "when the agent finishes, the work app takes the
  seat: walk, hop, then work" — `the pet never walked to the editor: [work]`.
- **처음 가설 (Windows 세션, 틀렸다)** `drainActivityEvents()`가 run loop를 돌리는 동안 런타임의
  자기 tick 타이머가 끼어들어 자리 인수인계가 녹화 전에 끝난다는 것. 이 테스트에서는 성립하지
  않는다 — `WorkAppScene`은 `runtime.start(drivingTicks: false)`로 켜고
  (`FocusActivityLogicTests.swift` `WorkAppScene.init`), `scheduleNextTick`을 부르는 곳은 `start`의
  `drivingTicks` 분기 · 포인터 상호작용의 `apply` · `tickTimerFired` 자신뿐이라
  (`RoamlingRuntime.swift`) 포인터 입력이 없는 이 장면에서는 타이머가 무장되지 않는다. 실패 메시지도
  맞지 않았다: 30초 동안 입은 그림이 `[work]` 하나였는데, 끝 신호가 닿았다면 인수인계가 아무리
  빨라도 축하·점프가 기록에 남는다.
- **원인 (맥에서 실측, 2026-09-18)** **`.achievement` 이벤트가 끝까지 배달되지 않는다.**
  - 배달 경로: `FakeAgent.emit` → `AsyncStream` → `RoamlingRuntime.swift`의
    `Task { for await event in stream }` → `handleActivityEvent`. `for await`의 `next()`는 메인 액터
    밖(협력 스레드 풀)에서 재개된 뒤 메인으로 돌아오므로, 배달에는 **풀 스레드가 실제로 스케줄되는
    시간**이 든다.
  - `drainActivityEvents()`(`RuntimeLogicTests.swift`)는 기한이 이미 지난
    `RunLoop.main.run(mode:before: Date())`를 400번 돌린다. 기다리지 않으므로 400번은 실제 시간으로
    거의 0이다. 그 안에 풀 스레드가 돌지 못하면 이벤트는 아직 메인 큐에 없다.
  - 이어지는 `WorkAppScene.run(seconds:)`은 가짜 시계와 `runtime.tick()`만 돌리고 run loop를 돌리지
    않는다. 그래서 **늦은 이벤트는 테스트가 끝날 때까지 오지 않는다** — agent가 끝나지 않았으니
    펫은 30초 내내 `work`다.
- **실측** 임시 카운터(emit 수 · `handleActivityEvent` 수 · `tickTimerFired` 수)를 넣고 쟀다. 카운터는
  커밋하지 않았다.
  - 무부하 20회: 전부 통과. 배달은 drain 1,143번 중 늦어도 21번째 회전에 끝났다(400 중).
  - CPU 14개에 `yes` 42개로 부하를 건 12회: **9회 실패.** B4와 같은 테스트·같은 메시지가 3회 나왔고,
    그 drain은 전부 `배달 안 됨 · 타이머 tick +0`이었다. 배달된 경우에도 최대 395번째 회전까지 밀렸다 —
    400은 여유가 아니라 우연이다.
  - **B4는 이 테스트 하나의 문제가 아니다.** 같은 원인으로 "a recorded session replays tick for tick"
    5회, "a hidden pet keeps agent activity but requests no luminance" 2회, "beside a working agent …"
    셋이 각 1회 떨어졌다. `drainActivityEvents()`로 agent 이벤트를 기다리는 테스트 전부가 대상이다.
    트레이스 테스트의 실패는 트레이스가 틀려서가 아니라 이벤트가 제 틱에 닿지 않아서다 —
    **트레이스를 다시 만들 이유가 아니다.**
  - 곁가지: 타이머 tick이 drain 중에 끼어드는 일은 실제로 있다. 다만 `runtime.start()`를
    `drivingTicks` 없이 부르는 두 테스트(`RuntimeLogicTests.swift` "arriving at a work seat ends the
    walk even with nothing to react with"와 그 아래 한 곳)에서만이고, 이번 실패들과는 무관하다.
- **상태** 수정됨(2026-09-18, 맥).
- **고친 것** 하네스 층만 — 런타임은 한 줄도 바꾸지 않았다.
  - `FakeAgent.makeEventStream()`(`RuntimeLogicTests.swift`)이 같은 이벤트를
    `AsyncStream(unfolding:)`으로 넘기면서, 런타임이 다음 이벤트를 달라고 온 횟수를 센다
    (`DeliveryCount`). 런타임의 `for await`는 이벤트 하나를 `handleActivityEvent`로 처리한 뒤에야 다음을
    달라고 하므로, **"달라고 온 횟수 = 보낸 수 + 1"이면 전부 처리된 것이다.** 런타임 안을 들여다보지
    않고 테스트가 볼 수 있는 유일한 배달 신호다. 런타임이 멈춰 스트림이 끝났거나 아무 런타임도 듣지
    않는 agent에게는 빚이 없다.
  - `drainActivityEvents()`는 살아 있는 `FakeAgent` 전부(`DeliveryLedger`)의 빚이 0이 될 때까지 run
    loop를 1ms 기한씩 **실제 시간으로** 돌린다. 그 뒤에 예전의 기한 지난 400회전을 그대로 둔다 —
    capture Task(`RoamlingRuntime.swift` `luminanceTask`)는 `@MainActor`끼리라 풀 스레드를 거치지
    않고, 기한 지난 회전으로 결정적으로 빠진다(`CaptureProviding`과 `FakeCaptureProvider` 둘 다
    `@MainActor`). 호출부 19곳은 그대로다.
  - 10초를 기다려도 안 오면 조용히 지나가지 않는다. `RoamlingLogicTestMain.swift`가 테스트 본문 뒤에
    `DeliveryLedger.takeUndelivered()`를 보고 그 테스트를 "an agent event was still undelivered"로
    실패시킨다. 테스트 자신의 단언이 먼저 떨어졌으면 그 밑에 같이 찍는다.
- **확인** 같은 부하(`yes` 42개)에서 하네스 **20회 연속 통과**(고치기 전 12회 중 9회 실패). 무부하 1회
  통과, 실행 시간 그대로(약 30초). `scripts/test.sh` 전체 통과. `RuntimeTrace.txt`는 건드리지 않았고
  바이트 비교 통과.
- **남은 것** `runtime.start()`를 `drivingTicks: false` 없이 부르는 두 테스트의 타이머 끼어들기는
  그대로다(위 "곁가지"). 지금까지 실패를 낸 적은 없다.

### B5. 맥에서 껐다 켜면 고른 색이 사라진다 (2026-09-18)

- **누가·언제** 사용자, 2026-09-18, 맥.
- **원문** "맥에서 껏다가 다시 켜면 색 유지가 안돼 선택창은 잘 선택되어 있는데 기본색인 보리 보리로
  돌아가."
- **원인 (코드로 확인, 미재현)** 저장은 된다. **시작할 때 읽은 색을 그림에 입히지 않는다.**
  - `RoamlingRuntime.init`은 `loadInitialAsset(...)`로 첫 그림을 먼저 만든다. 그 함수의 내장 펫 경로는
    `MascotPetFactory.make(builtInPet, images:)` — `sheets:` 없이, 즉 원본 색이다
    (`RoamlingRuntime.swift` `loadInitialAsset`).
  - 저장된 색은 그 **뒤에** 읽어 `palette`에만 넣는다(`init`의 `DefaultsKey.palette` 절). 메뉴의 체크는
    `paletteOptions`가 `preset.palette == palette`로 정하므로 선택은 맞게 보인다.
  - 다시 칠하는 `reinstallBuiltInPetForPalette()`를 부르는 곳은 `apply(palette:)` 하나뿐이고, 그
    함수는 `newPalette != palette`일 때만 나아간다. 그래서 **재실행 뒤 이미 체크된 같은 색을 다시 눌러도
    아무 일도 없다** — 다른 색을 거쳐야 돌아온다.
  - Windows는 시작할 때 복원한다(`rust/roamling-win/src/main.rs` 시작 시 팔레트 복원 절:
    `built_in_mochi_recolored(palette)`). 맥에만 그 절이 없다.
- **상태** 고침(2026-09-18, 사용자 "응 고쳐줘"). 서명 빌드(`build/Roamling.app`)를 다시 켜서 사용자가
  확인했다 — "잘 된다".
- **고친 것** `RoamlingRuntime.init`의 끝에서 저장된 색이 기본색이 아니면
  `reinstallBuiltInPetForPalette()`를 한 번 부른다. 패키지 펫이나 Mochi가 아닌 내장 펫 위에는 덮지
  않는다(그 함수의 guard). 기본색 사용자의 시작 경로와 저장 형식은 그대로다. 설명은
  `docs/palette.md` "색 기억 — 저장만 하고 입히지 않았다".
- **확인** 새 테스트 `a colour picked before quitting is the colour the pet comes back in`
  (`TuningPersistenceTests.swift`)이 오버레이에 닿은 픽셀을 비교한다. 고치기 전 런타임에서 "the pet
  came back in the bundle's colours"로 실패, 고친 뒤 통과. 하네스 198개와 `scripts/test.sh` 전체 통과,
  `RuntimeTrace.txt`는 그대로.

## 거절·보류

### 하늘 보리 — 밝은 파랑 프리셋 (드랍 2026-09-18)

- **어디서** R14를 정하던 중 "파란 보리로 하자 그럼 하늘 보리는 나중에 추가하고".
- **결정** 같은 날 밀린 일을 고르면서 "2번은 드랍". 프리셋을 늘리지 않는다. 이름만 남긴다 —
  밝은 파랑 프리셋을 정말 만들게 되면 그때 이 이름을 쓴다.

---

## 아직 안 물어본 것

아내분 설문에서 나왔지만 요청이 아니었던 관찰. 다음에 물을 때의 재료다.

- **아이들이 아주 좋아한다** (4번 "자주 나옴. 애들은 겁나 좋아함", 6번 "겁나 많이 잡고 가지고
  논데. 다시 잘 돌아다님"). 잡기·끌기가 예상 밖의 주 사용처다.
- 크롬은 일할 때도 있고 아닐 때도 있다 — 앱 단위 on/off로만 다루기로 했다(창 제목으로 가르지
  않는다).
